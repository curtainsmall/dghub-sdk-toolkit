[project]
name = "tetris-demo"
version = "0.1.0"
description = "俄罗斯方块 DGHub 插件示例 — Tetris demo plugin for DGHub"
requires-python = ">=3.11"
dependencies = [
    "pygame-ce>=2.5",
    "dghub-sdk",
]

[tool.dghub]
entry = "src/main.py"
